class EntityCategory {
  EntityCategory({
    required this.categoryId,
    required this.categoryName,
    required this.parentId,
  });

  String categoryId;
  String categoryName;
  int parentId;
}
